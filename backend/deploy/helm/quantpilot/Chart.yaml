apiVersion: v2
name: quantpilot
description: QuantPilot backend proxy — FastAPI + PostgreSQL + Redis + Vault Agent
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: QuantPilot Team
